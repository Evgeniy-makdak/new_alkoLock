/**
 * @deprecated Мобильная панель перенесена в ReportsPage (кнопка «Создать новый отчёт»).
 * Файл оставлен для совместимости импортов; не используется.
 */
export function ReportsMobileToolbar(): null {
  return null;
}
