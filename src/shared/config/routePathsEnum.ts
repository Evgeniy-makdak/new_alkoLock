export enum RoutePaths {
  all = '*',
  root = '/',
  auth = '/authorization',
  events = '/events',
  users = '/users',
  roles = '/roles',
  groups = '/groups',
  tc = '/tc',
  alkozamki = '/alkozamki',
  attachments = '/attachments',
  autoService = '/auto_service',
}
